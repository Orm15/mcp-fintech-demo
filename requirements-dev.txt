# Dev/test dependencies — kept independent of service requirements.
#
# Unit tests only need pydantic (for entities) and the test tools.
# Integration tests use testcontainers (docker) so the service deps
# are exercised through the docker build, not installed here.
#
# Note: psycopg2-binary lacks Python 3.13 wheels at time of writing —
# CI runs on Python 3.12 (see .github/workflows/ci.yml).

# Test framework
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0

# Service deps needed by tests (entities, MCP server import)
pydantic>=2.7.2,<3.0.0
httpx==0.28.1
asyncpg>=0.30.0
mcp[cli]==1.27.0
structlog==25.5.0

# Integration tests (require docker)
testcontainers[postgresql]==4.14.2

# Lint, typecheck, security
ruff==0.15.12
mypy==1.20.2
bandit==1.9.4
pip-audit==2.10.0
pre-commit==4.6.0

# Optional: install if you have Python 3.12 for postgres adapter tests
# psycopg2-binary==2.9.9
